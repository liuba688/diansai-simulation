# 电赛小车开发汇总

## 2026-07-30 巡线阶段验收

- 满负载实车完成整圈，约 18.0 s 检测到终点线，巡线阶段进入 20 s。
- 正式稳定档为直线 160 RPM、普通弯 92～115 RPM、巡线 KP=0.20、最大纠偏 75 RPM。
- 验收日志 `output/run_logs/run_20260730_233651.csv` 全程无 `mask=0`，未触发丢线搜索。
- 当前固件包含终点倒车寻线与 IMU 回正；最新可烧录 HEX 位于 `project/MSPM0G3507_Car/project/mdk/Objects/SeekFree_MSPM0G3507_Device_Library.hex`。
- 视觉模块和步进电机接入后，应以本版本为基线重新做完整负载重复性测试。

当前主工程把两条开发线合并为一套可从 OLED 菜单操作的控制程序：

- 使用八路灰度传感器循迹；全白时按最后偏差方向搜索 200 ms，随后停车。
- MaixCAM 完成钢球检测、目标跟踪和 UART2 通信。
- MSPM0G3507 完成视觉接近、盲走、轨迹倒放和重新捕线。
- MPU6050/MPU6500 提供航向角、静止角度保持和重新校准。
- 双编码器速度 PID 与里程计使用实车调试版本。
- 删除电磁铁控制和 HC-05 蓝牙控制，统一改用 OLED 四键菜单。
- Keil MDK 与 CCS 两套工程同时维护。

主工程位于 `project/MSPM0G3507_Car`，视觉端位于 `maixcam_ball_car`。往年题库保存在
`2024赛题复现`，今年 H 题原题和参考资料保留在仓库根目录。

详细架构、接线、部署和协议见 `docs/`。
