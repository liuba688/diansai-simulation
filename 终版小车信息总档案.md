# 终版小车信息总档案

当前软件基线为 7.29 融合版本。MSPM0G3507 负责灰度循迹、速度闭环、航向测量、
静止角度保持、里程计和视觉运动状态机；MaixCAM 负责钢球检测与跟踪。

唯一用户入口是 OLED 四键菜单。返回键是运行中的安全停车键。系统不使用电磁铁，
也不初始化 HC-05 蓝牙串口。

硬件引脚、状态机、部署步骤和协议分别见 `PCB接线与引脚速查.md` 与 `docs/`。
