# MSPM0G3507 终版小车调试日志

> 本文保留原始排错全过程；本人和队友的逐日工作总览见《开发历程合并时间线.md》。
> 已淘汰方案仍属于开发历程，不得从历史日志中删除。

> 本文件由旧《开发进度.md》完整迁移而来，用作过程备查档。以下原始记录不删减，仅补充归档说明与结论跳转索引。
>
> 原始记录存在“阶段性待办后来已经完成”“早期判断后来被推翻”等时间线现象；请以《开发进度.md》的当前结论为准。
>
> 当前有效状态（2026-07-30）：终版车已完成自有电机速度闭环、八路灰度接入和自适应巡线；HC-05 调试已正式放弃，当前使用 COM9 USB-TTL。

## 结论跳转索引

- 当前整体状态 → [开发进度.md：当前项目状态](开发进度.md#当前项目状态)
- 当前引脚映射 → [开发进度.md：当前硬件映射](开发进度.md#当前硬件映射)
- 当前 PI 与巡线参数 → [开发进度.md：当前有效控制参数](开发进度.md#当前有效控制参数)
- 当前巡线逻辑 → [开发进度.md：当前巡线行为](开发进度.md#当前巡线行为)
- 已完成事项 → [开发进度.md：已完成里程碑](开发进度.md#已完成里程碑)
- 当前任务与风险 → [开发进度.md：当前待办与风险](开发进度.md#当前待办与风险)

## 原始过程记录（按原时间顺序完整保留）

> 建立日期：2026-07-23  
> 当前阶段：开发与烧录链路验证

## 1. 固定协作方式

从现在开始采用以下分工：

1. Codex 负责直接修改工程代码，并在修改后进行力所能及的静态核对。
2. 用户负责在 Keil 中编译、烧录和进行实物测试。
3. Codex 每次修改完成后，必须明确说明：
   - 修改了哪些文件；
   - 修改了什么功能；
   - 用户下一步需要怎样编译、烧录和测试；
   - 预期现象是什么。
4. 用户将 Keil 的编译信息、下载信息和实物测试现象反馈给 Codex。
5. Codex 根据反馈继续修改代码，并更新本进度文件。
6. 除非用户另有要求，不再让用户手工输入或修改代码。

## 2. 工程位置

- 终版工程根目录：`D:\ai embody\TI preparation\project\MSPM0G3507_Car`
- Keil 工程：`D:\ai embody\TI preparation\project\MSPM0G3507_Car\project\mdk\SeekFree_MSPM0G3507_Device_Library.uvprojx`
- 主程序：`D:\ai embody\TI preparation\project\MSPM0G3507_Car\project\user\src\main.c`
- 逐飞原仓库保持不修改。

## 3. 已完成

- 已核验 Keil、Arm Compiler 6、MSPM0 SDK、SysConfig 和 MSPM0 DFP 的安装情况。
- Windows 已识别 Horco CMSIS-DAP v2，并曾枚举出虚拟串口 COM10。
- 已将完整逐飞模板复制到本项目工作区：
  - 共 685 个文件；
  - 约 22.35 MB；
  - 保留 `libraries`、`project` 及其相对路径结构；
  - 抽查文件的 SHA-256 与逐飞源仓库一致。
- Keil Flash Download 当前配置已确认：
  - `Erase Sectors`；
  - `Program`；
  - `Verify`；
  - `Reset and Run`；
  - `MSPM0G MAIN 128KB`；
  - Flash 地址范围 `0x00000000`～`0x0001FFFF`；
  - 算法 RAM 为 `0x20200000`，大小 `0x00008000`。
- 已将 `main.c` 改为最小 LED 闪烁程序：
  - PA14 推挽输出；
  - 每 500 ms 翻转一次；
  - 保留系统时钟初始化和调试串口初始化。
- 第一次编译结果为 1 个错误、2 个警告：
  - 错误位于 `main.c`，`gpio_toggle()` 未声明；
  - 原因是该名称仅在定义 `COMPATIBLE_WITH_OLDER_VERSIONS` 时作为旧版兼容宏存在；
  - 已改用当前库正式接口 `gpio_toggle_level()`；
  - 两个警告来自逐飞库 `zf_driver_pit.c` 的枚举类型比较，暂不影响 LED 链路测试。
- 修正后已经成功编译、烧录，板载 LED 正常闪烁。由此确认：
  - PA14 确实能够控制板载 LED；
  - Arm Compiler 6 可以编译当前工程；
  - Horco CMSIS-DAP 能识别目标芯片；
  - SWD 擦除、编程、校验、复位和程序运行链路正常。
- 已进入调试串口验证阶段：
  - 使用逐飞模板的 `debug_init()`；
  - UART：UART0；
  - 波特率：115200；
  - 逐飞空模板最初默认 TX=PA10、RX=PA11；
  - 程序每秒输出一次 `MSPM0G3507 heartbeat: LED loop running.`；
  - PA14 LED 继续每 500 ms 翻转，作为程序运行心跳。
- 第一次串口测试现象：
  - COM10 可以正常打开和监听；
  - 没有收到串口文本；
  - 用户反馈 LED 仍然亮着。
- 已根据终版载板原理图修正调试串口引脚：
  - 原理图 UART0 TX 为 PA0；
  - 原理图 UART0 RX 为 PA1；
  - `zf_common_debug.h` 已改为 `UART0_TX_A0` 和 `UART0_RX_A1`；
  - 波特率保持 115200。
- 用户确认上一次测试没有连接串口 TX/RX；补充连接并重新烧录后，VS Code 串口监视器仍无输出。
- VS Code 串口监视器截图显示：
  - 端口为 COM10；
  - 波特率为 115200；
  - 端口能够正常打开；
  - 接收区域没有数据。
- 按用户要求进行第二轮诊断修改：
  - GPIO 心跳从 PA14 改到 PB16；
  - PB16 每 500 ms 翻转一次；
  - 串口仍为 UART0、TX=PA0、RX=PA1、115200；
  - 输出由 `printf()` 改为直接调用 `uart_write_string()`，绕过 printf 重定向；
  - 每秒发送 `MSPM0G3507 heartbeat: PB16 loop running.`。
- 第二轮测试结果：
  - PB16 LED 正常闪烁，证明程序主循环持续运行；
  - VS Code 监听 COM10 仍无任何串口数据；
  - 因为直接 UART 发送和主循环均正常，当前优先怀疑 COM10 与 PA0 之间的物理连接、TX/RX 方向或 Horco 虚拟串口接口。
- 独立 USB-TTL 测试结果：
  - USB-TTL 枚举为 COM9；
  - PB16 LED 仍正常闪烁；
  - UART 仍无输出；
  - 测试时曾同时连接 GND、5V、RX、TX，并尝试过同名连接和交叉连接。
- 供电安全结论：
  - 当前载板已经由核心板 USB 供电；
  - USB-TTL 的 5V 必须断开，禁止与载板 5V 相连，避免双电源并联及反向供电；
  - 单向日志测试只接载板 GND 与 USB-TTL GND，以及载板 TX 与 USB-TTL RX；
  - 若 USB-TTL 的 TX 是 5V 电平，不得直接接入 MSPM0 的 RX。
- 已渲染并目视核对原理图，载板顶部 UART0 四针接口从 1 到 4 为：
  - 1：+5V；
  - 2：GND；
  - 3：UART0_RX_PA1；
  - 4：UART0_TX_PA0。
- 第三轮诊断固件改为同时初始化并发送四路 UART：
  - UART0：TX=PA0，RX=PA1，发送 `UART0 PA0 OK`；
  - UART1：TX=PB6，RX=PB7，发送 `UART1 PB6 OK`；
  - UART2：TX=PA23，RX=PA24，发送 `UART2 PA23 OK`；
  - UART3：TX=PB2，RX=PB3，发送 `UART3 PB2 OK`；
  - 四路均为 115200，且每秒发送一次；
  - PB16 LED 心跳保持不变。
- 第三轮四路 UART 测试全部通过：
  - UART0 PA0 正常；
  - UART1 PB6 正常；
  - UART2 PA23 正常；
  - 载板丝印 UART4 对应的 PB2（芯片 UART3）正常；
  - USB-TTL 使用 COM9；
  - PB16 LED 持续正常闪烁。
- 已明确区分“载板接口丝印编号”和“MSPM0 内部 UART 外设编号”：
  - 载板 UART4 实际对应芯片 UART3，信号为 PB2/PB3；
  - 载板 UART3 与 UART2 共用 PA23/PA24，但 UART3 带 12V；
  - 载板 UART5 与 UART1 共用 PB6/PB7，但 UART5 带 12V；
  - UART3/UART5 是四线电机兼容接口，本车不使用。
- 用户已更正此前信息：实物 UART2 供电脚为 5V，与现有 PDF 一致；总档案已同步修正。
- 四路 UART 诊断代码已收敛：
  - 后续固定使用 UART0，TX=PA0、RX=PA1、115200；
  - UART1、UART2、UART3 不再由当前程序初始化；
  - PB16 继续作为运行心跳。
- 已进入蜂鸣器测试阶段：
  - 原理图控制信号为 PA7；
  - TMB12A05 资料确认是 5V 电磁式有源蜂鸣器；
  - 程序暂按高电平触发；
  - 每 2 秒输出一个约 200 ms 的短鸣脉冲；
  - UART0 同步输出蜂鸣器测试日志。
- 蜂鸣器测试通过，用户确认模块丝印明确标注“低电平触发”。
- 已修正蜂鸣器控制逻辑：
  - PA7 默认输出高电平关闭蜂鸣器；
  - 每 2 秒拉低约 200 ms 短鸣；
  - 不再使用此前的高电平触发测试逻辑。
- 已进入 OLED 测试阶段：
  - 原理图 OLED SCL=PA31、SDA=PA28；
  - 使用逐飞软件 I2C，避免后续与 MPU6050 硬件 I2C 资源冲突；
  - 暂按常见 SSD1306 地址 0x3C、128x64 分辨率初始化；
  - OLED 显存每 2 秒在 0xAA/0x55 棋盘格之间切换；
  - UART0 输出 OLED 初始化和刷新日志；
  - PB16 LED 与蜂鸣器测试继续保留。
- OLED 测试通过：
  - 屏幕成功显示密集横纹图案；
  - 确认 SSD1306 地址 0x3C、128x64 初始化和 PA31/PA28 软件 I2C 通信正常。
- 蜂鸣器低电平触发逻辑测试通过；后续改为仅启动时短鸣一次。
- 已进入 MPU6050 通信测试阶段：
  - 原理图 MPU6050 SCL=PA1、SDA=PA0、AD0 接地；
  - 软件 I2C 地址为 0x68；
  - PA0/PA1 与原 UART0 冲突，因此调试串口迁移到 UART1；
  - 新调试串口为 UART1，TX=PB6、RX=PB7、115200；
  - 程序退出 MPU6050 睡眠，并每秒读取寄存器 0x75 `WHO_AM_I`；
  - 正常预期值为 0x68；
  - OLED 保留静态横纹图案，PB16 继续闪烁。
- 实际读取到 `WHO_AM_I = 0x70`。
- 根据 TDK InvenSense 官方 MPU-6500 寄存器手册，0x70 是 MPU-6500 的默认设备 ID；I2C 地址仍为 0x68。
- 当前模块应按 MPU-6500 或其兼容芯片处理，不再按标准 MPU6050 的 0x68 设备 ID 判断。
- 已进入 IMU 原始数据测试：
  - 采样率配置约 125 Hz；
  - 数字低通滤波开启；
  - 加速度量程 ±2 g；
  - 陀螺仪量程 ±250 dps；
  - 每秒读取一次加速度 XYZ 和陀螺仪 XYZ 原始值；
  - UART1 输出格式为 `ACC[x,y,z] GYRO[x,y,z]`。
- 用户提供的 7 组静止原始数据统计：
  - 加速度均值约 `[671, 182, 15177]`；
  - 加速度标准差约 `[61, 21, 25]` LSB；
  - 陀螺仪均值约 `[-282, 41, -56]` LSB，即 `[-2.15, 0.31, -0.43] dps`；
  - 陀螺仪标准差约 `[0.034, 0.067, 0.050] dps`；
  - 当前主要问题是稳定零偏，不是严重随机噪声，暂不需要更换模块。
- 已加入上电自动零偏校准：
  - 上电后要求小车水平、静止 4 秒；
  - 按 125 Hz 采集 500 组样本；
  - 自动计算加速度 XYZ 和陀螺仪 XYZ 偏置；
  - 水平校准时将 Z 轴目标设为 16384 LSB（+1 g）；
  - 校准完成后蜂鸣器短鸣一次；
  - 串口输出偏置和校准后的 `CAL ACC[...] GYRO[...]`。
- 用户提供的校准后静止数据表现良好：
  - ACC X/Y 约在 ±60 LSB 内；
  - ACC Z 围绕 16384 小幅波动；
  - GYRO 最大约 15 LSB，折合约 0.11 dps；
  - 当前 IMU 满足小车定角转弯和航向辅助的基础要求，无需更换。
- OLED 测试界面已升级为状态文字界面，校准完成后显示：
  - `TI CAR READY`；
  - `IMU: MPU-6500`；
  - `CALIBRATION: OK`；
  - `UART1: 115200`。
- 用户提供动画素材：`D:\ai embody\TI preparation\小猫.mp4`。
- 素材参数：720x720、30 FPS、约 15.77 秒。
- 已制作 OLED 小猫动画：
  - 截取约 2 秒的连贯动作；
  - 转换为 128x64 单色画面；
  - 小猫主体缩放到约 60x60 并居中；
  - 共 12 帧，播放约 6 FPS；
  - 动画数据占用约 12 KB Flash；
  - 生成文件为 `project/code/cat_animation.h`。
- 新的 OLED 流程：
  - 上电期间显示测试图案；
  - IMU 校准完成后显示状态文字约 1.5 秒；
  - 随后循环播放小猫动画；
  - 动画播放期间 PB16 心跳和 IMU 串口数据仍继续运行。
- 小猫动画实机测试通过，播放流畅。
- 已将当前可直接编译烧录的完整娱乐版工程另存为：
  - `D:\ai embody\捂嘴喵\MSPM0G3507_Car`
  - Keil 入口：`D:\ai embody\捂嘴喵\MSPM0G3507_Car\project\mdk\SeekFree_MSPM0G3507_Device_Library.uvprojx`
  - 原始素材：`D:\ai embody\捂嘴喵\小猫.mp4`
  - 共 851 个文件，约 26.83 MB；
  - 已核对 `.uvprojx`、`main.c` 和 `cat_animation.h` 的 SHA-256，均与当前已验证版本一致。
- 八路灰度模块当时状态（已被 2026-07-26 结果取代）：
  - 当时尚未接入终版 MSPM0G3507 载板；
  - 该模块的巡线功能已经在初版四 TT 电机小车上通过实车验证；
  - 旧车验证结果可以复用扫描逻辑、黑线有效电平和误差计算思路；
  - 上述项目后来均已在终版车验证完成。
- 正式主工程已退出小猫动画播放；娱乐版仍完整保存在 `D:\ai embody\捂嘴喵`。
- 已建立独立 TB6612 双电机底层：
  - 新增 `project/code/tb6612.c` 和 `tb6612.h`；
  - PWMA=PA12/TIMG0_C0；
  - AIN1=PB19、AIN2=PB17；
  - PWMB=PA13/TIMG0_C1；
  - BIN1=PA16、BIN2=PB24；
  - PWM 频率暂定 20 kHz；
  - 有符号占空比接口范围为 -10000～10000；
  - 正数和负数控制两个方向，0 为滑行停止；
  - 当时 `TB6612_OUTPUT_ENABLE=0`；当前正式工程已经设为 1 并完成实车巡线；
  - 主程序上电初始化 TB6612 后保持双通道 PWM=0；
  - 当时 OLED 状态界面显示 `MOTOR: LOCKED`；
  - Keil 工程文件已加入 TB6612 源文件。
- TB6612 安全基线实机验证通过：
  - PB16 正常闪烁；
  - OLED 正常显示；
  - 电机输出仍保持锁定。
- 已建立双编码器软件正交解码模块：
  - 新增 `wheel_encoder.c/.h` 并加入 Keil 工程；
  - MOTOR1：A=PA25、B=PA14（旧档案曾误记为PA24/PA25，已纠正）；
  - MOTOR2：A=PA26、B=PA27；
  - 四根信号均使用 GPIO 双边沿中断；
  - 使用 16 项正交状态转移表进行四倍频有符号累计；
  - 当前无需主电源，可在 USB 供电下手动转动车轮测试；
  - UART1 每秒输出 `ENC MOTOR1=... MOTOR2=...`；
  - OLED 显示 `ENCODER: READY`；
  - 当时尚未定义左右轮；当前已确认左轮=MOTOR1+TB6612 B、右轮=MOTOR2+TB6612 A。
- 首次双编码器手动测试结果：
  - MOTOR1 计数只能在 0 和 1 之间变化，无法连续累计；
  - 该现象高度符合 A/B 两相中只有一相产生边沿的情况；
  - MOTOR2 计数正常；
  - MOTOR2 对应电机向小车前进方向转动时计数减小，反向转动时计数增大；
  - MOTOR1 实际左右轮映射和正方向仍未确认。
- 已加入编码器逐相信号诊断：
  - 分别累计 MOTOR1 A、MOTOR1 B、MOTOR2 A、MOTOR2 B 的中断边沿数；
  - 同时输出两路当前 AB 电平；
  - UART 格式为 `EDGE M1[A=... B=... AB=..] M2[A=... B=... AB=..]`；
  - 用于区分编码器本体/线束故障与载板接口/MCU引脚故障。
- 逐相诊断实测结果：
  - `M1[A=0 B=12271 AB=11]`；
  - `M2[A=4882 B=4881 AB=00]`；
  - 按旧错误映射监听时，PA25有信号而PA24没有信号；
  - MOTOR2 A/B 两相边沿数几乎完全一致，证明软件正交解码逻辑正常；
  - 该现象后来通过重新核对原理图确认是引脚映射错误，并非据此证明硬件损坏。
- 交换两个完整 6Pin 电机插头后，零边沿现象仍固定在软件所监听的 PA24，初步看似载板问题。
- 重新直接目视核对 2026-06-13 原理图后发现旧档案引脚记录有误：
  - MOTOR1 实际编码器信号为 PA25/PA14，不是 PA24/PA25；
  - MOTOR2 为 PA26/PA27；
  - 之前 PA24 始终为0、PA25持续产生边沿，正好符合“PA24未连接、PA25为有效编码器相”的实际情况；
  - 当前不应判定载板损坏。
- 已将 MOTOR1 软件正交解码改为 A=PA25、B=PA14，待重新实测。
- 两只电机的编码器方向已按统一机械视角确认：
  - 观察/摆放基准为电机位于内侧、车轮位于左侧；
  - 在该基准下，两只电机正转时编码器计数均减小，反转时计数均增大；
  - 该结论描述的是单体电机机械视角，不能直接等同于装车后的“向前”为正或为负；
  - MOTOR1/MOTOR2 与整车左轮/右轮的最终映射，以及装车向前时各自的计数符号，仍需在底盘安装后确认。
- 新增 STM32 麦轮 PID 参考工程：
  - 路径：`D:\robotmaster pj\RM PID\F401_PID 7.16\F401_PID-DIR\F401_PID-DIR`；
  - 重点文件：`User/app_task.c`、`bsp_encoder.c/.h`、`drv_tb6612.c/.h`、`Direction.c/.h`；
  - 该工程使用 100Hz 速度采样、四电机独立 PID、目标换向时清空 PID 状态、目标为0时停止输出；
  - 编码器参数写为电机轴每圈11脉冲、AB四倍频、减速比21.3，因此其输出轴理论计数为 `11×4×21.3=937.2`；
  - 上述21.3减速比只能作为当前 JGB680 的待验证线索，不能直接写入正式参数；
  - 可借鉴速度计算公式、独立控制器结构和换向清积分思路；STM32 HAL/定时器映射、四麦轮运动学、PID参数不能直接移植；
  - 源码含单轮1.855/0.98经验倍率、停止目标写0.01、直接使用浮点CMSIS PID等项目特例，正式工程不照搬。
- 输出轴每圈计数建议用轮胎与固定支架双基准线测量；连续手转5或10圈后用总计数差除以圈数，可显著降低人工停位误差。
- 当前两只 JGB680 输出轴十圈计数实测：
  - MOTOR1：起始 `-5252`，十圈后 `19461`，变化 `+24713`，即 `+2471.3 count/rev`；
  - MOTOR2：起始 `386`，十圈后 `-24371`，变化 `-24757`，即 `-2475.7 count/rev`；
  - 两路绝对每圈计数的平均值为 `2473.5 count/rev`，相差44计数/十圈，约0.18%，一致性良好；
  - 若编码器确为11脉冲/电机轴转且软件采用AB四倍频，则推算减速比约为 `2473.5/(11×4)=56.22:1`，很可能是标称56:1版本，仍需客服确认；
  - 本次两路转动方向相反，因此计数一正一负；计数绝对值用于每圈标定。
- 学长已将 STM32 麦轮项目配套电机一并提供，可在必要时作为整套替换电机使用；若确认其确为11PPR、21.3:1版本，可以连同其编码器换算参数作为起点，但仍需核对额定电压、电流、输出轴/安装尺寸及 TB6612 电流余量。
- 曾准备双电机一次性正反转测试流程，但复核档案后确认当前无法给电机安全供电：
  - 主电源开关尚未到货/安装，3S 电池到 12V、板载降压及 5V 网络的完整链路尚未验证；
  - 已立即恢复 `TB6612_OUTPUT_ENABLE=0`，任何测试命令都会被强制为 PWM=0；
  - 正反转测试代码暂时保留但被安全锁完全屏蔽，不得在供电链路验证前解除；
  - 本轮没有生成或烧录解除安全锁的新固件，实物仍未受到电机驱动。

## 4. 当时需要用户执行（历史记录，已完成）

1. 将 USB-TTL 调试线从载板 UART0 移到 UART1：
   - 载板 UART1 第 2 针 GND → USB-TTL GND；
   - 载板 UART1 第 4 针 PB6/TX → USB-TTL RX；
   - UART1 第 1 针 5V 不接；
   - UART1 第 3 针 PB7/RX 不接；
   - USB-TTL TX 不接。
2. 在 Keil 中 Rebuild，确认 0 Error 后烧录 MPU6050 测试固件。
3. USB-TTL 仍使用 COM9、115200、8N1、无流控。
4. 当前载板由核心板 USB 供电，USB-TTL 不接 5V。
5. 上电后的前 4 秒必须让小车水平、完全静止。
6. 校准结束时蜂鸣器短鸣一次，PB16 LED随后持续闪烁。
7. 串口应输出 `BIAS ACC[...] GYRO[...]` 和 `IMU calibration complete.`。
8. 静止时观察校准后的数据是否接近 `CAL ACC[0,0,16384] GYRO[0,0,0]`。
9. 分别倾斜和转动小车，确认加速度和陀螺仪数值会明显变化。
10. 确认 OLED 在校准完成后从横纹切换为四行英文状态文字。
11. 确认状态文字显示约 1.5 秒后，OLED 循环播放小猫动画，动作方向和画面无明显错位。
12. 重新编译正式工程，确认 TB6612 模块无编译错误。
13. 当时的测试要求为：烧录后确认 OLED 显示 `MOTOR: LOCKED`、PB16 闪烁、IMU 日志正常，且未接主电源时不驱动电机。
14. 确认两只电机 6Pin 插头按丝印接入 MOTOR1/MOTOR2。
15. 仅使用核心板 USB 供电，不连接 3S 电池。
16. 分别只转动一只车轮，确认串口只有对应 MOTOR1 或 MOTOR2 计数变化。
17. 记录每只轮向车头前进方向转动时计数是增加还是减少。
18. 重新烧录逐相诊断固件，分别转动两只轮，记录四个 A/B 边沿累计值。
19. MOTOR1 正常时 A/B 边沿数应同时持续增加且数量接近；若其中一路保持0或极小，即可定位缺失相。
20. 将两个电机插头恢复到原来的 MOTOR1/MOTOR2 位置。
21. 重新编译并烧录 PA25/PA14 修正版。
22. 分别转动两只轮，确认 MOTOR1 和 MOTOR2 的 A/B 边沿数都同时增加且相互接近。

## 5. 本轮预期结果

- 编译结果为 `0 Error(s)`。
- Flash 下载及校验成功。
- PB16 上的测试 LED 保持约 1 秒的完整闪烁周期。
- 串口工具每秒收到一行 `MSPM0G3507 heartbeat: PB16 loop running.`。

## 6. 当时待确认事项（历史记录）

- Horco CMSIS-DAP 的虚拟串口是否已经通过物理线路连接载板 PA0/PA1。
- 当前所用 Horco 线束是否包含虚拟串口 TX、RX 和 GND，还是只连接了 SWD。

## 7. 当时规划顺序（现已完成）

LED 和下载链路验证成功后，依次进行：

1. 调试串口周期输出；
2. 主电源路径安全检查；
3. 蜂鸣器、OLED 和 MPU6050 单模块验证；
4. 架空测试左右电机；
5. 验证左右编码器及计数方向；
6. 实现左右轮速度闭环；
7. 接入八路灰度并实现基础循迹。

---

## 2026-07-29 OLED 四键菜单实机排错与速度调整

### 四键菜单

- 初始菜单代码误把按键暂定为 PB8/PB9/PB10/PB11，其中 PB8/PB9 没有方便的载板丝印接口，方案不可用。
- 后续一度尝试修改 `TI preparation` 中的同名旧工程，但用户实际使用的是 `D:\ai embody\diansai start!\diansai-simulation`，因此多次编译烧录后车上仍运行旧菜单。Keil 本身没有问题，根因是两个工作区存在同名工程。
- 根据载板 H56 连续引脚，最终确定 UP=PB13/H56-4、DOWN=PB23/H56-3、OK=PB26/H56-2、BACK=PB27/H56-1，按键公共端接 GND。
- MOTOR2 编码器是 PA26/PA27；菜单键是 PB26/PB27。曾误将 A/B 端口混淆，复核后确认互不冲突。
- 使用只显示原始 GPIO 电平、禁止电机动作的 `KEY TEST 0730` 诊断固件逐键测试。用户确认四路均为松开 `1`、按下 `0`，OLED 显示与按键操作正常。
- 诊断结束后 `CAR_MENU_KEY_DIAGNOSTIC_MODE` 恢复为 `0`，正式菜单重新启用。按键扫描仍保留上电记录松开电平的兼容机制。

### 巡线速度

- `LINE_FOLLOW_STRAIGHT_RPM` 保持 70 RPM。
- 新增 `LINE_FOLLOW_MAX_CURVE_RPM=50 RPM`，普通弯基础速度固定/限制为 50 RPM。
- `LINE_FOLLOW_SHARP_OUTER_RPM` 由 28 RPM 提高到 50 RPM，急弯内轮保持 -12 RPM。
- 正确工程 Keil 编译通过：`Code=47568, RO-data=3068, RW-data=224, ZI-data=8352`，0 error、0 warning。
- 本次 50 RPM 弯道参数尚未实车验收；首次测试需检查冲出赛道、急弯摆动、丢线恢复和 TB6612/电机温升。

---

## 2026-07-24 当前补充进度：学长车 PID 预验证结束，准备切换到自己的电机

### 当时工程策略

- 原正式工程仍保持不直接修改。
- 已按要求将完整工程复制到 `D:\ai embody\TI preparation\before the final car\MSPM0G3507_Car`，并只在该副本内进行学长车阶段的代码测试。
- 当前测试副本入口为：`D:\ai embody\TI preparation\before the final car\MSPM0G3507_Car\project\mdk\SeekFree_MSPM0G3507_Device_Library.uvprojx`
- 保留功能：PA7 低电平触发蜂鸣器、MPU/IMU 校准、OLED 状态显示、PB16 心跳、UART1 调试输出、双编码器软件正交解码。

### 学长车阶段已经完成的代码工作

- 已新增速度闭环模块：`project/code/speed_pid.h`、`project/code/speed_pid.c`。
- 已启用 TB6612 输出：`TB6612_OUTPUT_ENABLE = 1`。
- 已将 `speed_pid.c` 加入 Keil 工程。
- 已实现左右两路独立速度闭环测试流程：IMU 校准、蜂鸣器短鸣、停车 3 秒、`+30 RPM`、停车、`-30 RPM`，并通过 UART1 输出目标速度、测得速度、PWM duty 和编码器增量。

### 当时 PID 与测速参数状态

- 学长车硬件参数暂用：11 PPR、AB 四倍频、21.3:1、937.2 count/rev。
- 该历史测试副本当时使用基础循环 10 ms、速度统计与 PID 控制窗口 50 ms；正式巡线工程当前为 20 ms 速度 PI。
- 当前副本磁盘上的 PID 参数：`KP=10.0`，`KI=0.0`，`KD=0.0`，`SPEED_PID_OUTPUT_LIMIT=3000`，`SPEED_PID_START_DUTY=1000`，`SPEED_PID_FILTER_ALPHA=0.35`。
- 已加入起步占空比补偿：实际输出为“目标方向上的起步 duty + PID 修正量”，用于解决低速时纯 P 输出太小、无法克服电机静摩擦的问题。

### 实测现象与结论

- 用户提供的日志显示，早期使用较大增益时，在 `target=-30` 附近出现 PWM 在 `+3000/-3000` 之间饱和翻转，速度读数也在大幅正负跳变。
- 主要原因：10 ms 速度窗口下低速计数颗粒太粗；直接用 RPM 作为 PID 单位时学长工程的 `35/10` 增益过大；两路电机在相同 PWM 符号下的编码器原始方向相反，需要分别修正符号。
- 之后已改为 50 ms 测速窗口并增加低通滤波。
- 当 `KP=1, KI=KD=0` 时电机不动，判断为正常：30 RPM 误差只会产生约 30 duty，远低于电机起步死区。因此加入 `SPEED_PID_START_DUTY` 起步补偿。

### 编译状态

- 最近一次 Keil 命令行编译通过：Arm Compiler 6.24，Program Size 为 Code=26408、RO-data=2524、RW-data=212、ZI-data=5516，结果为 `0 Error(s), 0 Warning(s)`。

### 供电与烧录器连接结论

- 可以同时接烧录器和外部电源，但必须避免两个电源同时给同一条 3.3V/5V 网络供电。
- 推荐连接：外部电源/电池给 PCB 和电机供电；烧录器只接 GND、SWDIO、SWCLK、NRST、VTref/3V3 检测；烧录器的 target power / 3.3V 输出应关闭，或不接供电脚。
- 烧录器 GND 必须与 PCB GND 共地，否则 SWD 信号没有共同参考。

### 当时下一阶段：切换到自己的电机（现已完成）

- 用户准备从学长车电机切换回自己的最终车电机。
- 切换后不能继续默认使用学长车的 `937.2 count/rev` 和 `21.3:1` 参数。
- 自己电机此前实测输出轴计数约为：MOTOR1 `2471.3 count/rev`，MOTOR2 `2475.7 count/rev`，平均 `2473.5 count/rev`。
- 当时建议把速度换算参数从学长车的 `937.2` 改为自己电机参数并重新做架空测试；该步骤后来已完成，正式程序最终采用商家 `2450 count/rev`。
- 切换后必须重新确认：MOTOR1/MOTOR2 对应左轮还是右轮；正向 PWM 时车轮是否朝整车前进方向转；两路编码器在整车前进方向下的计数符号；两轮最低起步 duty；30 RPM、60 RPM 等低速目标下的实际 `d50` 和 `rpm_x100` 是否合理。

### 切换自己电机后的首轮测试建议

1. 车轮架空，手放电源开关旁。
2. 先只测试开环低 duty，例如 600、800、1000、1200，记录两轮刚好能稳定转动的 duty。
3. 再启用速度闭环，先用很低目标速度，例如 `target=20 RPM` 或 `target=30 RPM`。
4. 正常情况下，使用 `2473.5 count/rev` 和 50 ms 窗口时，30 RPM 对应 `d50` 约为 `62`，20 RPM 对应 `d50` 约为 `41`。
5. 如果 PWM 迅速饱和到 `±3000` 且速度符号反向，优先检查该路编码器符号或电机方向，不要继续长时间运行。

---

## 2026-07-24 自有电机规格补充

### 用户原始补充

- 减速比为 56:1。
- 转速为 178 RPM。
- 四倍频由单片机设置；编码器本身只有 AB 两相，基础参数为 `11×56` 线/输出轴圈。
- 额定电流 1 A。
- 堵转电流 2 A。
- 额定转矩 6.5 N·m，最大转矩 9 N·m。
- 编码器供电推荐 5 V。

### 参数核对

- 按 AB 两相四倍频计算，理论输出轴计数为 `11×56×4=2464 count/rev`。
- 此理论值与此前两轴十圈实测均值 `2473.5 count/rev` 接近，差约 0.39%；闭环当前继续采用实测值，理论值用于规格核对。
- 额定 1 A、堵转 2 A 需要继续核对 TB6612 的持续电流、峰值电流、散热条件和堵转保护余量。
- 编码器用 5 V 供电时，需要确认 AB 输出到 MSPM0 GPIO 的电平兼容性，不能仅凭供电电压推定输出一定安全。
- 2026-07-24 后续确认：转矩单位为 N·m。

→ 最终结论跳转：对应开发进度.md中【编码器-缺相假象与计数标定】、【电机驱动-安全锁与供电】。

---

## 2026-07-24 PCB 接线资料归档

- 已依据 `PCB\TI板卡\原理图文件\SCH_Schematic1_2026-06-13.pdf` 提取网络并目视核对整页原理图。
- 已用 `PCB\TI板卡\Gerber2\10.zip` 作为最终 PCB/丝印版本参考，不再以 Gerber 推断网络归属。
- 已建立根目录 `PCB接线与引脚速查.md`，集中记录电源、UART、TB6612、电机编码器、蜂鸣器、OLED、IMU、舵机、RGB、扩展 IO、80 Pin 核心板映射及易错点。
- 对原理图未明确标注的 H56-1、辅助排针用途保留“不确定/需实测”标记，未擅自补全。

→ 最终结论跳转：对应开发进度.md中【项目位置】及根目录《PCB接线与引脚速查.md》。

---

## 2026-07-25～26 自有电机闭环、HC-05 与八路灰度巡线

- 正式闭环改用商家 `2450 count/rev`；左右映射确认为左轮=MOTOR1(PA25/PA14)+TB6612 B、右轮=MOTOR2(PA26/PA27)+TB6612 A。
- 30 RPM 闭环误差约 1%；20/30/60/100 RPM 架空测试均稳定，最高 duty 不到 6000。
- HC-05 接板载 UART4（实际 MCU UART3 PB2/PB3），9600 baud；无线日志和单字符 `1/0` 启停均实机通过。
- YB-MVX05 正式接入：A0=PB25、A1=PB18、A2=PB21、OUT=PB22，黑线有效为高电平。
- X1～X8 使用 `-350～350` 对称权重；传感器位于驱动轮轴线前约 7～8 cm。
- 初版固定 55 RPM 且采用 `base±correction`，急弯时外轮反而加速，曾出现来不及转向而驶离黑线。
- 最终改为动态弯道降速、外轮限速、内轮可停止/反转、最外侧急弯状态和按最后方向丢线搜索。
- 该阶段直线基线为 55 RPM，普通弯最低 20 RPM、急弯外/内轮 28/-12 RPM、丢线搜索外/内轮 22/-10 RPM，搜索 200 ms 后停车；随后直线已实车提升到 70 RPM。
- 正式巡线速度 PI 周期为 20 ms，KP=10、KI=1.0、KD=0，滤波 `alpha=0.25`；历史 50 ms 参数不再是当前配置。
- HC-05 发送 `1` 后等待 3 秒并持续巡线，发送 `0` 立即停车。
- 相关单元通过 Arm Compiler 6 编译检查；用户实车确认“现在巡线完美”，基础巡线验收完成。

---

## 2026-07-26 角度测量、蓝牙修复与 IMU 硬件故障

### 角度模块开发

- `Car_angle` 新增 `mpu6050_yaw.c/h`、`angle_pid.c/h`、`odometer.c/h`，并已加入 Keil 工程。
- 原模块板面标为 MPU6050，但实测 `WHO_AM_I=0x70`，按 MPU6500 兼容器件工作；I2C 地址 0x68，软件 I2C 使用 PA1=SCL、PA0=SDA。
- 由于模块在车上并非水平安装，单独积分陀螺仪 Z 轴会固定比例少算。修正方案为：校准三轴陀螺仪零偏和静止重力单位向量，再将三轴角速度投影到车体竖直轴后积分。
- TIM_G12 实测日志持续为 `dt=10.0 tk=0`，没有提供可信的自由运行时间基准，因此没有继续依赖该定时器修正。
- 根据安装状态标定采用比例系数 1.268。用户手动转动结果：90°显示 89.7°，达到当前 ±3°目标；继续手动转动一圈累计 372.2°。一圈数据受手动角度基准影响，暂不据此再次改比例，待新模块用固定角尺或赛道直角复标。

### HC-05 乱码与不稳定

- 角度详细日志开启后蓝牙出现 `���` 等乱码和间歇不稳定。
- 处理：格式化日志缓冲区由 128 字节增至 256 字节，避免长行造成栈/缓冲区越界；高频完整 IMU 遥测仅发送 UART1，HC-05 仅发送命令短应答。
- 用户随后确认通信恢复正常。HC-05 仍接板载 UART4（MCU UART3 PB2/PB3），9600 baud。

### 停车抗扰测试失败与定位

- 增加 `h` 命令用于显式开启低速停车角度保持，`0` 停车；初始限制最大约 18 RPM、角度死区 ±1.5°。
- 首次测试车体被拨动后完全不修正。日志显示 `yaw/rate/gyro` 全为 0，随后诊断字段显示 `who=FF`、`ae=0`、`ac=0`，说明并非 PID 参数或方向问题，而是 IMU 已无 I2C 响应。
- 启动日志最终为 `ERROR IMU NOT FOUND`，用户拔下模块后发现明显发烫。板上 LED 亮只代表供电存在，不能证明传感器芯片正常；当前按模块损坏处理，禁止再次接回。
- 固件已增加 `WHO_AM_I` 最多 20 次重试；只有识别成功才校准。故障时 OLED 显示 IMU 错误、禁用航向保持，`h` 返回失败，避免在无角度反馈时驱动电机。
- 最新固件编译通过：0 error、0 warning；镜像统计 `Code=33048, RO=3196, RW=220, ZI=5604`，HEX 时间 20:41:22。

### 替换件与复测顺序

- 选用标准 GY-521 MPU6050 模块，要求与旧板相同的八针顺序：`VCC, GND, SCL, SDA, XDA, XCL, AD0, INT`；建议购买 2～3 块备用。
- 不选 GY-87、GY-25、QMI8658 或 ICM-20602：外形、接口协议或寄存器驱动均不能作为当前排母和程序的直接替换。
- 插入前必须逐针核对 VCC 对 VCC，不能仅因针序相同而忽略排母方向；反插可能再次烧毁模块。
- 新 GY-521 很可能返回 `WHO_AM_I=0x68`。到货后先让驱动兼容 0x68 和 0x70，再仅接控制板上电，确认供电、电流、温升和 I2C 响应正常，最后才接电机。
- 复测顺序：静止校准 → ±90°手动测角 → 分段 360°测角 → 停车抗扰修正方向 → 停车回正性能 → 行驶航向保持 → 灰度触发 90°转弯 → 编码器定距停车。

---

## 2026-07-27 更换传感器并合入角度环

- 已更换灰度传感器和 MPU6050；新硬件的具体型号、供电和信号定义仍以实物核对结果为准。
- 角度环源码合入主线，保留原巡线与 2024 赛题实现。
- MPU 驱动改为同时接受 `WHO_AM_I=0x68/0x70`。
- 旧 IMU 专属的1.268比例系数未复用，新模块曾临时恢复为1.000；后续已完成±90°与360°标定，当前正式值为1.172。
- 新灰度传感器确认输出方式、黑白有效电平、左右通道顺序和安装位置前，不修改原 `line_sensor` 参数，也不进行整车带电机巡线。

### 新灰度传感器恢复巡线

- 烧录后 OLED 灰度位图持续显示 `M00`，发送 `1` 后运行状态和延时状态正常，但巡线不动作。
- 最终确认不是角度环冲突，也不是灰度有效电平问题，而是新灰度传感器的 `OUT` 信号线漏接。
- 补接 `OUT→PB22` 后可以正常检测黑线，整车巡线恢复正常。

### 当前仍未解决的问题

- HC-05 接收方向正常：手机发送 `1/0/h/r/d/?` 等指令能够被小车执行；发送方向异常：MCU 返回手机的纯 ASCII 仍显示为 Unicode 替换字符 `�`。
- 已核对正式工程中 PB2=UART3 TX、PB3=UART3 RX，没有发现与 MPU、OLED、灰度或角度计时器的引脚复用冲突。
- 新 MPU6050 不发热，静止时角度可长期保持 0；但多次执行角度保持后会逐步累计误差，最终达到十余度。
- 代码检查发现 `TIM_G12` 定时器加载值为 `0xFFFF`，约 65.536 ms 回绕一次，而角度模块使用 32 位无符号差值处理回绕；回绕时可能把约 10 ms 的周期错误限制成 50 ms。此项尚未修改，需在烧录条件恢复后修复并复测。

## 2026-07-29 正式切换 CY-DXJ8 八路并行灰度

- 根据铺线识别和抗干扰实测体验，撤销此前“不使用 CY-DXJ8”的阶段性结论，正式从 YB-MVX05 地址扫描切换到 CY-DXJ8 `OUT1～OUT8 + 5V + GND`。
- 新映射为：OUT1=PB25、OUT2=PB18、OUT3=PB21、OUT4=PB22、OUT5=PA30、OUT6=PB0、OUT7=PB1、OUT8=PB14。
- 对应载板 H55-1、2、3、4、5、6、7、10；该接线继续沿用。PB10/PB11 已不再作为当前菜单启动和急停键。
- `line_sensor` 已改为八路并行GPIO读取，保留 `-350～350` 权重、掩码格式和巡线外环接口。
- 2026-07-29 已实测：白色地面为低电平，黑线为3.3 V高电平。因此 `LINE_SENSOR_ACTIVE_LEVEL=1` 和GPIO下拉配置正确，OUT可直接接MSPM0，不需要分压或电平转换。
- 实物方向确认：从车头朝前看OUT8最左、OUT1最右。软件读取表已反向排列为 `bit0=OUT8` 到 `bit7=OUT1`，保持原巡线算法的左负右正约定。
- 新模块从20 RPM重新进行全赛道验收，旧模块70 RPM结果只作为历史参考。
- 首次切换后Keil链接出现4个 `L6218E Undefined symbol pid_test_serial_*`。根因不是CY-DXJ8引脚或并行驱动，而是 `pid_test_serial.c/.h` 已存在且 `car_app.c` 正在调用，但文件未加入 `.uvprojx`；现已补入Keil工程源码组。

## 2026-07-29～30 USB-TTL PID 采集、编码器故障与自动调参

### 串口与硬件排查

1. COM17 是板载 Type-C CH340；COM9 是外接 USB-TTL。
2. COM9 TX/RX 回环成功；原 UART1 始终无协议响应，改用 MCU UART3 PB2/PB3（载板标 UART4）后握手、启停和遥测正常。
3. 曾遇端口占用、插头接触不良、设备消失和 `ClearCommError`；重新插接、关闭串口监视器并确认 COM9 后解决。
4. 初测左轮反馈接近 0、输出持续增大，稳态绝对误差约 25 RPM，饱和约 58%。更换异常的左轮电机/编码器组件后，得分由约 26～28 提升到约 78。
5. 6 针电机座外侧两针是电机线，中间四针才是编码器 `+5V/A/B/GND`。

### 自动调参与高速结果

| Kp | Ki | Kd | 25 RPM 得分 |
|---:|---:|---:|---:|
| 10 | 1.0 | 0 | 81.8 |
| 12 | 1.0 | 0 | **82.3** |
| 14 | 1.0 | 0 | 81.7 |
| 12 | 0.8 | 0 | 80.8 |
| 14 | 0.8 | 0 | 81.1 |
| 12 | 1.0 | 0.15 | 81.6 |

- 最佳 `12/1/0` 已写入固件默认值。10 RPM 得分 81.1、左右差 0.188 RPM；40 RPM 约 81.8。
- 限幅 8000 时：80 RPM 约 59.5 分、饱和约 59%；100 RPM 约 49.8 分、饱和约 76%。
- 限幅升到 9000 后最近一次 80 RPM 约 77.6 分，但稳态窗口偏短，需同一电池状态下延长复测。
- 结论：高速瓶颈包含电机、电池、驱动和机械能力，不能靠无限提高 PID 或限幅解决。

### 下地结果

- 赛道约 6.142 m，实车一圈约 50 s；当前参数理论约 32 s（含 3 s 启动延迟）。
- 优先缩短启动延迟、提高普通弯速并减少错误急弯/反转和丢线；蓝牙链路正式放弃。

```powershell
.\tools\pid_test\.venv\Scripts\python.exe .\tools\pid_test\pid_test.py --port COM9 --baud 115200 --duration 12 --target 80
```

运行前架空并关闭其他串口程序；紧急时按 `Ctrl+C`，同时保留物理断电手段。

## 2026-07-30 HC-05 调试方案终止

- 为无线回传目标/实际左右轮速度、`line_mask`、`line_error`、普通/急弯/丢线状态及累计时间，曾将 PID 遥测扩展为 `@PID,2` 并尝试通过 HC-05 发送。
- Windows 能建立 SPP 配对和短时连接，但多次出现虚拟 COM `121` 超时；连接成功时 MCU→电脑方向仍只收到 `�`、`���` 等乱码，无法完成 `PING/READY` 握手。
- 分别尝试 9600 和 115200 固件后问题仍存在；结合 2026-07-26 已记录的“手机→小车正常、MCU→手机乱码”，判定 HC-05 反向链路不适合作为当前调试基础设施。
- **最终决定：放弃 HC-05 调试，不再为其修改固件或占用联调时间。** HC-05 应从板载 UART4 断开。
- 当前正式调试链路为 COM9 USB-TTL ↔ 板载 UART4（MCU UART3 PB2/PB3），115200，TX/RX 交叉、GND 共地、不接 TTL VCC。
- 如后续确需无线回传，优先另立 ESP32 Wi-Fi UART→UDP/TCP 网桥方案；完成独立供电、带宽、丢包和控制周期影响测试前，不替代 USB-TTL 基线。
## 2026-07-30 启动延迟、普通弯速度与分状态计时

- 按交接建议将正式菜单任务启动延迟从 300 个 10 ms 周期改为 50 个周期，即从 3 s 降为 0.5 s。PID 串口阶跃测试的独立 1 s 安全延迟未改。
- 普通弯上下限同步由 50 RPM 提高到 65 RPM，直线 80 RPM、急弯 50/-12 RPM、丢线 22/-10 RPM 均保持不变。
- 原遥测只把普通巡线合并为 `normal`，无法量化直线和普通弯。本次按自适应基础速度是否已从 80 RPM 下调来区分二者，新增四类累计耗时和状态进入次数。
- 停车路径先发送一次 `@LINESTAT,1,total=...,straight=...,curve=...,sharp=...,lost=...,entries=...`，随后立即清零，避免重复 STOP 重发旧摘要。
- `@PID` 协议升级至 v3，PC 解析、CSV、JSON 和控制台摘要同步增加 `straight/curve/sharp/lost`；仍兼容旧 v1/v2。
- 全程只使用 COM9 USB-TTL；没有恢复 HC-05 或加入任何蓝牙代码。
- Keil 编译结果：Code=49232、RO-data=3356、RW-data=224、ZI-data=8392，0 error、0 warning。

### 极限速度试验参数

- 用户要求直接测试极限：直线由 80 RPM 改为 100 RPM，普通弯最大速度由 65 RPM 改为 80 RPM。
- 普通弯最低速度保留 65 RPM，避免较大误差下仍强制保持 80 RPM；急弯和丢线参数不变。
- 该参数组合尚未实车验证，测试时必须预留安全区并监控饱和率、供电压降和温升。

### 新电池 120 RPM 与 RAM 离线日志

- 旧电池标称/满电约 12.5 V，当前已降至 10.8 V；更换 12.5 V 新电池后继续提速。
- 确认速度 PI 输出限幅仍为 9000。本轮将直线设为 120 RPM、普通弯设为 75～90 RPM，没有同时修改限幅。
- 新增紧凑 RAM 缓冲区，速度环每5次记录一次，即10 Hz。加入偏航角速度字段后，1200条会超出当前RAM链接区；为保留栈和运行余量改为600条、最长60秒，满后停止写入并设置overflow标志。
- 普通巡线期间不再发送实时 `@PID,3`，PID 专项测试仍保留实时帧。停车时先执行 `tb6612_stop_all()`，再输出 `@LINESTAT` 和 `@RUNLOG`，避免长串口传输延迟急停。
- 日志不会因断开 USB-TTL 而丢失；停车后重新连接并发送 `@PIDTEST,DUMP` 可再次下载，但断电或开始下一次巡线会清除。
- PC 下载命令：`.\tools\pid_test\.venv\Scripts\python.exe .\tools\pid_test\download_run_log.py --port COM9`。
- 编译结果：Code=49912、RO-data=3516、RW-data=224、ZI-data=32400，0 error、0 warning。

### 日志驱动的弯速迟滞优化

- `run_20260730_141822.csv` 共367条、100 ms等间隔，无缺失。有效运动约36.3秒：普通弯33.2秒、急弯3.1秒、直线0秒、丢线0秒。
- 左右输出最大约4626/4459，没有接近9000；说明继续提速首先应解除误判限速。
- 普通弯判定新增90进入、60退出迟滞。小误差不再立即封顶90 RPM；强度50时基础请求约115 RPM。
- 急弯状态在日志中形成31个短片段，因此确认/退出均改为50 ms。
- 未直接启用绝对角度环接管。当前轮距未知，新IMU比例未复标；纯角度轨迹对轮滑和入弯初始偏差敏感。下一圈日志增加 `yaw_rate_dps`，后续据此建立目标偏航角速度前馈与阻尼。
- 最终编译：Code=49992、RO-data=3516、RW-data=224、ZI-data=22800，0 error、0 warning。

### 几何参数与IMU Angle Hold诊断修复

- 用户实测：轮距209 mm，灰度预瞄218～220 mm，探头间距10 mm，总跨度70 mm；轮子带载10圈前进1950 mm。
- 计算有效轮周长195 mm、轮径约62.07 mm，里程计换算已更新。标准500 mm半径下内外轮速度比约0.654。
- 用户反馈Angle Hold起始不为0，静止后角度仍小幅漂移并驱动车轮。代码确认进入任务时此前把当前累计Yaw直接设为目标，运行期间又统一调用gyro-only快速更新。
- 修复：Angle Hold进入时调用 `mpu6050_yaw_set_angle(0)`，目标设为0；Angle Hold期间调用完整更新，使静止零偏自适应生效。普通巡线继续快速更新。
- 曲率前馈暂不启用，先验证静止60秒漂移、手动±90°和顺逆时针360°；之后用新日志中的 `yaw_rate_dps` 建立目标角速度。
- 编译：Code=50032、RO-data=3516、RW-data=224、ZI-data=22800，0 error、0 warning。

### IMU加入标准半圆普通弯控制

- 新IMU的0°、90°、360°测量均已通过验收。
- 根据轮周长195 mm、轮距209 mm和半径500 mm计算：中心90 RPM对应线速度292.5 mm/s、目标偏航角速度约33.5°/s，内外轮约71.2/108.8 RPM。
- 普通弯启用几何半差前馈；灰度以KP=0.04、KD=0.05继续小幅修正。复核发现横向误差符号不等于曲率方向，因此正式顺时针任务固定使用右转前馈，避免误差过零导致前馈方向翻转。
- IMU闭环比较偏航角速度幅值，低于目标时增大轮速差，高于目标时减小；增益0.20、修正限幅6 RPM。实测角速度低于2°/s时先由轮速前馈建立转动。
- 急弯与丢线不叠加普通弯IMU控制，保持原状态机优先。
- RUNLOG DATA升级为v3，CSV增加实际/目标偏航角速度。
- 编译：Code=50660、RO-data=3520、RW-data=224、ZI-data=22808，0 error、0 warning。

### 手贴/标准赛道对比与状态机重构

- 手贴赛道日志344条，有效行驶约34.0秒：直线21.9、普通弯7.0、急弯5.1、丢线0秒，状态切换107次。
- 标准赛道日志293条，有效行驶约28.8秒：直线18.7、普通弯6.0、急弯2.3、丢线1.8秒，状态切换109次。
- 两圈输出最高约5800，未接近9000；标准赛道已进入30秒，但普通弯段中位长度仅0.1秒，确认前馈按误差反复开关是卡顿主因。
- 两圈 `yaw_rate_target_dps` 全0。代码核对确认 `mpu6050_yaw_init()` 只初始化、不校准，只有菜单CALIBRATE YAW会置 `is_calibrated=1`。
- 重构：关闭旧急弯入口；圆弧进入门限需连续80 ms，进入后记录IMU总Yaw，至少160°且灰度连续50 ms居中才退出。
- 正常轮速命令每10 ms最大变化4 RPM；丢线搜索/停车保持立即响应。
- LINE ONLY/BALL VISION在IMU未就绪时自动静止校准，失败不启动。
- 编译：Code=51140、RO-data=3552、RW-data=224、ZI-data=22824，0 error、0 warning。

### 新状态机首次直线右转故障

- 新日志仅56条。启动时前四条静止且mask24/error0；运动后约0.1秒出现mask25/error-116。
- 控制周期内该异常复合图样持续80 ms，圆弧锁存；下一日志已变为state1、目标角速度33.5°/s，随后左右目标最高约114/66 RPM，车辆在直线上被强制右转。
- mask25为分离多点命中，不符合单根黑线的连续成片图样，符合A点启停横线/交叉图样特征。
- 修复：运动后前50个10 ms周期禁止圆弧识别；新增掩码连续性检查，非连续图样不累计入弯确认。
- 编译：Code=51272、RO-data=3548、RW-data=224、ZI-data=22832，0 error、0 warning。

### 直线右转归因更正与安全回退

- 用户确认在启停线之后释放，因此此前将mask25判断为启停线图样没有证据。
- 根因修正为：横向误差只描述黑线相对传感器的位置，直线偏移和圆弧入口都可能产生相同大误差，不能用它触发固定曲率前馈。
- 关闭自动圆弧融合；恢复灰度误差连续转向和75～90 RPM降速，保留急弯禁用、目标斜坡、丢线保护及日志。
- 下一版需知道释放点到第一个圆弧切点的距离，以里程阶段锁定标准直线/半圆。
- 编译：Code=51116、RO-data=3552、RW-data=224、ZI-data=22832，0 error、0 warning。

### 5 cm启停线自动停车

- 用户改为在起点基准线前少量距离释放。若第一次看到横线立即停车会无法起跑，因此采用首次起点化、第二次停车。
- 判定：`active_count>=5`连续3个10 ms周期。首次确认后 `odometer_reset()`；550 cm前禁用终点。
- 一圈后第二次确认先调用停车路径，再统一导出日志。串口分别输出 `@FINISH,1,ARMED` 和 `@FINISH,1,STOP`。
- 编译：Code=51428、RO-data=3616、RW-data=224、ZI-data=22832，0 error、0 warning。

### MPU6050 双向整圈比例标定

- 完成菜单 `CALIBRATE YAW` 后，顺时针90°三次读数为-74°、-73°、-78°，逆时针90°三次为+85°、+88°、+85°；均能准确回到原朝向。
- 顺时针360°三次归一化残差为+44°、+48°、+47°，对应平均积分约-313.7°；逆时针360°残差约-57°～-61°，对应平均积分约+301°。
- 顺、逆时针单独比例分别约1.148和1.196，取折中值，将 `MPU6500_YAW_CALIBRATION_SCALE` 从1.000改为1.172。
- Keil编译通过：Code=50060、RO-data=3520、RW-data=224、ZI-data=22800，0 error、0 warning。下一步以相近速度完成顺逆时针各三次360°复测。
- 烧录复测结果：顺时针90°约-90.7°，逆时针90°约+89.6°，回到原朝向仅有零点几度残差；比例、方向和短时回零均通过验收，当前保留比例1.172，不再继续追调。
- 上述复测在 `Angle Hold` 模式下完成；正反向90°响应及返回原朝向均符合预期，因此本轮 `Angle Hold` 已通过实车验收。
- 正式符号约定：从车体上方俯视，逆时针Yaw为正、顺时针Yaw为负。当前新模块I2C地址0x68、SCL=PA1、SDA=PA0，角度比例1.172；驱动兼容WHO_AM_I 0x68/0x70，但新模块实测身份值尚未单独留档。

### 入弯滞后、启停线放宽与PI释放修正

- `run_20260730_181153.csv`：317条，100 ms周期；丢线66条。入弯时目标约90/64.5 RPM而实际仍约127.7/125.3 RPM，PWM没有及时从高速直线工况卸载。
- 将启停横线确认由 `active_count>=5`连续30 ms改为 `active_count>=4`连续20 ms。仍采用首次标记起点、550 cm后第二次停车。
- 初次加入目标下降时按新旧目标比例缩放PI存量输出，编译Code=51572，0 error、0 warning。
- `run_20260730_192755.csv`：251条，有效运动约24.7 s；丢线仅1条，入弯目标90/66.5时实际约75.5/62.9 RPM，说明快速卸载解决了高速冲弯。
- 同一日志暴露副作用：直线居中、目标120/120 RPM时实际平均约81.9/79.3 RPM；左右输出峰值约5067/5063，未受9000限幅约束。原因是每次灰度目标下降都按比例缩放存量输出，频繁小波动累积成明显失速和卡顿。
- 当前修正为有界释放：每下降1 RPM释放8 PWM，每次最大80 PWM；正常巡线加速斜率4 RPM/10 ms不变，减速斜率降至2 RPM/10 ms。丢线安全目标仍立即执行。
- 最近日志起点有一次 `mask=126`（6路命中），后程没有4路及以上采样。因此暂不继续降到3路，下一圈先确认灰度板是否物理扫过终点横线。
- 下一次日志需附从运动开始计时的直线、入弯、完全进弯、出弯、完全回直线与卡顿时刻（±0.5 s可用），用于构建持续确认和迟滞状态，而非固定时间脚本。
- 最新编译：Code=51648、RO-data=3620、RW-data=224、ZI-data=22832，0 error、0 warning。

### 满负载最终验收：巡线进入20秒

- 验收日志：`run_20260730_233651.csv`，共219条；用户确认满负载整圈任务完美完成。
- 约18.0 s出现跨左右半区的终点宽线，随后进入终点停车对齐；巡线计时进入20 s。
- 全程没有`mask=0`，没有进入55/20 RPM正转搜索或35/-20 RPM反转搜索。
- 直线160 RPM目标居中段实际平均约122.9/121.0 RPM，弯道92～115 RPM档稳定通过。
- 最终有效修复是将目标减速提高到10 RPM/10 ms，并把大幅PI释放门槛降到单步8 RPM，使满载高速入弯时PWM能够及时卸载。
- 本版本冻结为视觉模块和步进电机搭载前的巡线稳定基线。
